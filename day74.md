Day 74 – Directory Size Analyzer

What I Learned

- Used the "-d" test operator to verify that directories exist.
- Used "du -sh" to display directory sizes in a human-readable format.
- Stored multiple directory paths in a Bash array.
- Used a "for" loop to analyze several directories automatically.

Challenges Encountered

- There were no major issues. The script successfully detected each directory and displayed its size.

Resolution

- I verified that the script correctly checked each directory and reported its disk usage.

DevOps Lesson Learned

Monitoring directory sizes helps identify storage issues before they become serious. Automating these checks makes routine system administration faster and more reliable.
